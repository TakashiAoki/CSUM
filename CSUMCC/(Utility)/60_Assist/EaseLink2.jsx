// EaseLink Ver.2.00// Copyright (c) 2007-2018 Over Ray Studio・Takashi Aoki @voyager_vision. All rights reserved.// LastUpDate 2018/10/02// 複数プロパティのアニメーション値を0〜100に置き換えて補間率を同期させますvar curScriptName = "EaseLink";// **** Main Script ***************************************************************************************************************var selectPropertyList = new Array();scriptExecute( myCSUMCCToolsFolder.fsName + "/" + "getSelectedProperty.jsx" );if ( selectPropertyList.length > 0 ){	app.beginUndoGroup( curScriptName );	setEaseLink();	app.endUndoGroup();}// **** FUNCTION ******************************************************************************************************************//		EaseLink適用		function setEaseLink(){		scriptExecute( myCSUMCCToolsFolder.fsName + "/" + "getSelectedLayer.jsx" );				var CR = String.fromCharCode( 13 );//改行コード		var expEaseLink =		"// "+ curScriptName +CR+		"El = thisComp.layer("+"\""+"EaseLink_ctrl"+"\""+").effect("+"\""+"EaseLink"+"\""+")("+"\""+"ADBE Slider Control-0001"+"\""+");" +CR+		"if ( 1 < numKeys )" +CR+		"{" +CR+		"	St = thisProperty.key( 1 ).time;" +CR+		"	Et = thisProperty.key( numKeys ).time;" +CR+		"	thisProperty.valueAtTime( El/100*(Et-St)+St );" +CR+		"}" +CR+		"else" +CR+		"{ thisProperty; }";				for ( var i = 0; i < selectPropertyList.length; i++ )		{			var curLayer = selectPropertyList[i].propertyGroup(selectPropertyList[i].propertyDepth);			var curPropertyName = selectPropertyList[i].name;			if ( selectPropertyList[i].propertyType != PropertyType.PROPERTY || selectPropertyList[i].parentProperty.name == "EaseLink" ) continue;						//キーフレームが2個以上無い場合のAlertの出し方考察			if ( selectPropertyList[i].numKeys < 2 )			{				alert				(					"キーフレームが2つ以上必要です。" +CR+					"Comp : '" + activeCompName + "'" +CR+					"Layer : " + curLayer.index + " ('" + curLayer.name + "')" +CR+					"Property : '" + curPropertyName + "'"				);				continue;			}									if ( selectPropertyList[i].expressionEnabled != true )			{				createEaseLinkCtrl();//EaseLink_ctrlを作成								selectPropertyList[i].expression = expEaseLink;			}		}}// **** FUNCTION ******************************************************************************************************************//		EaseLink_ctrlを作成		function createEaseLinkCtrl(){		var curlayers = activeComp.layers;		for( var n = 1; n <= curlayers.length; n++ )		{			//#EaseLinkタグレイヤーが既にあった場合			if ( curlayers[n].comment == "#EaseLink" )			{				//スライダ制御エフェクトがあるか調べる				var scflag = null;				var curEffectParade = ctrlShape.property("ADBE Effect Parade");				for ( var p = 1; p <= curEffectParade.numProperties; p++ )				{					if ( curEffectParade[p].matchName == "ADBE Slider Control" ){ var scflag = true; break;}				}				if ( scflag == true ){ break; }			}			//#EaseLinkタグレイヤーが無かった場合、新たにEaseLink_ctrlを作成			if ( n == curlayers.length )			{				ctrlShape = curlayers.addShape();//新規シェイプレイヤー作成				ctrlShape.name = "EaseLink_ctrl";				ctrlShape.comment = "#EaseLink";				ctrlShape.position.expression = "[0,0]";				var curFx = ctrlShape.property("ADBE Effect Parade").addProperty("ADBE Slider Control");				curFx.name = "EaseLink";				curFx("ADBE Slider Control-0001").setValueAtTime( 0 , 0 );//Slider				curFx("ADBE Slider Control-0001").setValueAtTime( activeItem.duration-activeItem.frameDuration , 100 );			}		}}// **** FUNCTION ******************************************************************************************************************//		スクリプトファイルの実行		function scriptExecute( scriptFilePath ){		var scriptFileName = new File( scriptFilePath );		scriptFileName.open();		eval(scriptFileName.read());		scriptFileName.close();}